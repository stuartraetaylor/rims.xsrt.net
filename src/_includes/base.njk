<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{{ title }}{% if title %} | {% endif %}Wheel offset and fitment comparison tools</title>

<script type="text/javascript" src="/scripts/jquery/jquery-3.7.1.min.js"></script>

<script type="text/javascript" src="/scripts/common.js"></script>
<script type="text/javascript" src="/scripts/garagelsd.offset.js"></script>

<link rel="stylesheet" type="text/css" media="all" href="/style/main.css" />
<link rel="stylesheet" type="text/css" media="all" href="/scripts/style.offset.css" />
</head>

<body class="bg-[--color-page-bg] text-[--color-text] font-sans text-sm leading-relaxed">

<!-- Navigation -->
<nav class="fixed top-0 w-full bg-[--color-nav-bg] z-50">
  <div class="max-w-6xl mx-auto px-4 flex items-center justify-between h-10">
    <span class="text-[--color-nav-link] font-bold text-sm hidden md:block">RIMS</span>
    <!-- Hamburger (mobile) -->
    <button id="nav-toggle" class="md:hidden text-[--color-nav-link] text-2xl leading-none p-1"
            aria-label="Menu" aria-expanded="false">&#9776;</button>
    <!-- Nav links -->
    <ul id="nav-menu" class="
      hidden md:flex md:gap-4 md:items-center
      absolute md:static top-10 left-0 w-full md:w-auto
      bg-[--color-nav-bg] md:bg-transparent
      flex-col md:flex-row
      font-bold text-sm
    ">
      <li id="nav-offset">
        <a href="/tools/offset" class="block px-4 py-3 md:py-0 text-[--color-nav-link] hover:underline {% if navActive == 'offset' %}underline{% endif %}">
          Split Rim Offset
        </a>
      </li>
      <li id="nav-fitment">
        <a href="/tools/fitment" class="block px-4 py-3 md:py-0 text-[--color-nav-link] hover:underline {% if navActive == 'fitment' %}underline{% endif %}">
          Wheel Fitment
        </a>
      </li>
      <li id="nav-equivalents">
        <a href="/tools/equivalents" class="block px-4 py-3 md:py-0 text-[--color-nav-link] hover:underline {% if navActive == 'equivalents' %}underline{% endif %}">
          Equivalent Arch Clearance
        </a>
      </li>
    </ul>
  </div>
</nav>

<!-- Main content -->
<main id="content" class="entry-content max-w-6xl mx-auto px-4 pt-14 md:pt-16">
  <h1 class="sr-only">{{ title }}</h1>
  {{ content | safe }}
</main>

<script>
// Hamburger menu toggle
document.getElementById('nav-toggle').addEventListener('click', function() {
  var menu = document.getElementById('nav-menu');
  menu.classList.toggle('hidden');
  if (menu.classList.contains('hidden')) {
    this.setAttribute('aria-expanded', 'false');
  } else {
    menu.classList.add('flex');
    this.setAttribute('aria-expanded', 'true');
  }
});

// Mobile tab switching
(function() {
  var tabs = document.querySelectorAll('.mobile-tab');
  var panels = document.querySelectorAll('.tab-panel');
  if (!tabs.length) return;

  function switchTab(targetId) {
    tabs.forEach(function(t) {
      var isActive = t.getAttribute('data-tab') === targetId;
      t.classList.toggle('border-[--color-nav-link]', isActive);
      t.classList.toggle('text-[--color-text-dark]', isActive);
      t.classList.toggle('border-transparent', !isActive);
      t.classList.toggle('text-gray-400', !isActive);
    });
    panels.forEach(function(p) {
      if (window.innerWidth < 1024) {
        p.classList.toggle('hidden', p.id !== targetId);
        p.classList.toggle('block', p.id === targetId);
      }
    });
  }

  tabs.forEach(function(tab) {
    tab.addEventListener('click', function() {
      switchTab(this.getAttribute('data-tab'));
    });
  });

  // Auto-switch to Results tab on form submit (mobile only)
  var form = document.querySelector('#offset-calculator form');
  if (form) {
    form.addEventListener('submit', function() {
      if (window.innerWidth < 1024) {
        setTimeout(function() { switchTab('tab-results'); }, 100);
      }
    });
  }

  // On resize to desktop, show all panels
  window.addEventListener('resize', function() {
    if (window.innerWidth >= 1024) {
      panels.forEach(function(p) {
        p.classList.remove('hidden');
        p.classList.add('block');
      });
    }
  });
})();
</script>

</body>
</html>
